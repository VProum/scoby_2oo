import React from "react";
import Map from "./../components/Map"

const Home = (props) => {
  // Implement react map box here.
  return (
    <div>
      <Map/>
    </div>
  );
};

export default Home;
